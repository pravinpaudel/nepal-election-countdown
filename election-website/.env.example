# Site Configuration
NEXT_PUBLIC_SITE_URL=http://localhost:3000

DATABASE_URL="postgresql://user:password@localhost:5432/election_db?schema=public"

# SMTP Email Configuration

SMTP_HOST="smtp.gmail.com"
SMTP_PORT="465"
SMTP_SECURE="true"
SMTP_USER="email@gmail.com"
SMTP_PASS="app-specific-password"
SMTP_FROM="Nepal Election <your-email@gmail.com>"

# Admin API Key (for protected endpoints like /api/subscribers/count)
ADMIN_API_KEY="secure-random-key-here"