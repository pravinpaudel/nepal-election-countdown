const config = {
  // use the standard Tailwind postcss plugin + autoprefixer for compatibility
  // Tailwind v4 uses the separated postcss plugin package
  plugins: ["@tailwindcss/postcss", "autoprefixer"]
};

export default config;
